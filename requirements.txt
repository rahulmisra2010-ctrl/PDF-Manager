# Flask stack
Flask~=3.0.0
Flask-SQLAlchemy~=3.1.0
Flask-WTF~=1.2.0
Flask-Bcrypt~=1.0.1
Flask-Login~=0.6.3

# PDF processing (PyMuPDF — no PyTorch required)
PyMuPDF>=1.23.0
# pdfplumber: layout-aware PDF text extraction (used as primary extractor in training)
pdfplumber>=0.9.0

# Export formats
openpyxl~=3.1.0
python-docx~=1.1.0

# Utilities
python-dotenv~=1.0.1
Werkzeug~=3.0.0

# AI / IDP extraction — Mindee Intelligent Document Processing API
mindee>=4.0.0

# AI / IDP extraction — Koncile.ai document extraction SDK (module: koncile_sdk)
koncile>=0.1.0

# OCR — Tesseract wrapper and PDF-to-image conversion for scanned PDFs
pytesseract>=0.3.10
Pillow>=10.0.0
pdf2image>=1.16.0

# LLM-based extraction — OpenAI GPT for intelligent field parsing
openai>=1.0.0