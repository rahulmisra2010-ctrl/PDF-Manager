# Flask stack
Flask~=3.0.0
Flask-SQLAlchemy~=3.1.0
Flask-WTF~=1.2.0
Flask-Bcrypt~=1.0.1
Flask-Login~=0.6.3

# PDF processing (PyMuPDF — no PyTorch required)
PyMuPDF>=1.23.0
# pdfplumber: layout-aware PDF text extraction (used as primary extractor in training)
pdfplumber>=0.9.0

# Export formats
openpyxl~=3.1.0
python-docx~=1.1.0

# Utilities
python-dotenv~=1.0.1
Werkzeug~=3.0.0

# OCR fallback for image-based PDFs (EasyOCR + dependencies)
easyocr~=1.7.2
opencv-python~=4.13.0.92
torch~=2.10.0
torchvision~=0.25.0